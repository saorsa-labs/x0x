{
  "id": "c76e841c",
  "title": "Phase 5: Auto-fixes applied — DONE",
  "tags": [
    ">doc-audit",
    "phase-5"
  ],
  "status": "completed",
  "created_at": "2026-05-11T21:16:18.372Z"
}

## Applied fixes

### Version numbers (3 files)
- README.md: `x0x = "0.16"` → `"0.19"`
- SKILL.md: `version: 0.19.32` → `0.19.41`
- CHANGELOG.md: Only goes to v0.19.31 — **NOT fixed** (would require adding entries for v0.19.32–v0.19.41; this is a content decision, not a simple update)

### Stale claims (2 files)
- CLAUDE.md: "No justfile exists yet" → updated to note justfile exists
- CLAUDE.md: Phase C.2 "designed not yet implemented" → "landed"
- src/bin/x0x.rs: CLI help "70 REST API routes" → "130 REST API routes"

### ADR staleness (1 file)
- docs/adr/0004-quic-stream-and-channel-limits.md: Updated channel/stream limits from 1,024/10,000 to 50,000/50,000 with historical context preserved

### CLAUDE.md accuracy (1 item)
- CLAUDE.md: "128 REST endpoints" → "130 REST endpoints (114 unique paths)"

## Issues NOT auto-fixed (require judgment call)
1. **CHANGELOG.md gap**: Only goes to v0.19.31 but current is 0.19.41 — needs manual content addition
2. **CLAUDE.md module diagram**: Missing `dm.rs`, `dm_capability.rs`, `connectivity.rs`, `contacts.rs`, `trust.rs`, `exec/`, `files/`, `hedge.rs`, `constitution.rs` — updating would be a significant change to the architecture section
3. **Missing ADR for GSS**: The Group Shared Secret decision has no ADR — should be added as a new ADR, not an edit
4. **API reference gaps**: `/health`, `/introduction`, `/ws`, `/ws/direct`, `/ws/sessions` not in docs/api-reference.md — would require adding ~15 table rows
5. **CLAUDE.md API endpoint count**: Still says "128" in one place? Let me double-check...

## Verification
- `cargo clippy --all-targets --all-features -- -D warnings` ✅ clean
- `cargo nextest run --all-features --workspace` ✅ 1,174/1,174 passed, 143 skipped
