# DEPRECATED: WASM Build Workflow
#
# This workflow has been consolidated into .github/workflows/build.yml
# WASM builds now run as part of the main multi-platform build matrix.
#
# See build.yml matrix entry:
#   - platform: wasm32-wasi
#     target: wasm32-wasip1-threads
#
# This file is kept for historical reference and will be removed in a future PR.

name: WASM Build (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "⚠️  This workflow is DEPRECATED"
          echo ""
          echo "WASM builds are now part of .github/workflows/build.yml"
          echo "Use the main build workflow instead."
          exit 1
