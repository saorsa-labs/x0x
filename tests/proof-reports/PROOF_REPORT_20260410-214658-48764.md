# x0x E2E Proof Report

**Generated:** 2026-04-10 20:46:58 UTC
**Run ID:** 20260410-214658-48764
**Proof Token:** `proof-20260410-214658-48764`
**Binary:** /Users/davidirvine/Desktop/Devel/projects/x0x/target/release/x0xd
**x0x version:** unknown

---

## Summary

| Suite | Status | Pass | Fail | Skip |
|-------|--------|------|------|------|

**Total:** Pass=0  Fail=0

---

## Verified Agent IDs (PROOF — extracted from live APIs)

These IDs were read directly from running x0xd daemons — not generated or guessed:


---

## Proof Methodology

Every test assertion in this report was validated by:

1. **Round-trip verification** — a unique token (`proof-20260410-214658-48764`) was embedded in sent
   data and verified in the response (proves real API interaction, not mock data).

2. **ID cross-verification** — agent IDs extracted from REST endpoints were verified
   to match CLI output and appear in peer discovery lists of other nodes.

3. **CRDT convergence** — KV store values and task list states written by one agent
   were read back by a second agent after sync delay (proves gossip works).

4. **Live API extraction** — all IDs, versions, and state values shown above were
   extracted from actual HTTP responses using `python3 -c "...json.load(sys.stdin)..."`.

5. **Exit code validation** — every test suite's exit code was checked; non-zero exit
   codes are counted as failures regardless of log content.

---

*This report was generated by `tests/e2e_proof.sh` — a deterministic test runner
that fails loudly on any discrepancy between sent and received data.*
